version: '3'
services:
  ka:
    build:
      context: .
      args:
        ARCH: ${ARCH:-amd64}
    working_dir: /work
    volumes:
      - ./:/work
    command: bash -c "python3 src/convert.py --log INFO --version 3.1.0-dev DocumentDefinitions/*.json"
