version: 2
jobs:
  build:
    machine: true
    parallelism: 1
    steps:
      - checkout
      - run:
          docker-compose up ka
      - store_artifacts:
          path: dist
      - store_artifacts:
          path: build/Templates/Kwaliteitsplan/Template-Kwaliteitsplan.md
