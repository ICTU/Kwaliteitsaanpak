services:
  ka:
    build:
      context: .
    environment:
      VERSION: ${VERSION:-wip}
    working_dir: /work
    volumes:
      - ./:/work
    command: bash -c "uv run src/convert.py --log INFO DocumentDefinitions/*.json"
