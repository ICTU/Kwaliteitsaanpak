version: 2
jobs:
  build:
    machine:
      image: ubuntu-2004:current
    parallelism: 1
    steps:
      - checkout
      - run:
          docker-compose up
      - store_artifacts:
          path: docs/wip
