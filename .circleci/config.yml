version: 2
jobs:
  build:
    machine: true
    parallelism: 1
    steps:
      - checkout
      - run:
          docker-compose up
          ls
      - store_artifacts:
          path: ICTU-Kwaliteitsaanpak-Full.pdf

