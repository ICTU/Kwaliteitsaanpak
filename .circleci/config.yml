version: 2.1
jobs:
  build:
    machine:
      image: ubuntu-2204:2024.11.1
    parallelism: 1
    steps:
      - checkout
      - run: docker compose up
      - store_artifacts:
          path: docs/wip
